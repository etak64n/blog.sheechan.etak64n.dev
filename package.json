{
  "name": "blog-shiichan",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply blog-shiichan --local",
    "db:migrate:remote": "wrangler d1 migrations apply blog-shiichan --remote"
  },
  "dependencies": {
    "hono": "^4",
    "jose": "^6",
    "marked": "^16",
    "zod": "^4"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4"
  }
}
